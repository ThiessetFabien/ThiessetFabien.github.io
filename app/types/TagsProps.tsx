/**
 * Tags interface.
 * @typedef {Object} Tags
 * @property {string} tag - The name of the tag.
 * @example
 * const tag: Tags = { tag: 'JavaScript' };
 */

export interface Tags {
  tag: string;
}
