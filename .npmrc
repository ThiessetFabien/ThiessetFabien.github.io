registry=https://registry.npmjs.org/
access=public
engine-strict=true

# Note: Les configurations suivantes ont été retirées car obsolètes avec npm 11+
# resolution-mode=highest (anciennement)
# network-timeout=100000 (anciennement)
