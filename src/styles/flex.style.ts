/**
 * A CSS class string for a flex container with full width,
 * items aligned at the center, and content justified between.
 */
export const cnFlexBetweenX: string =
  'flex w-full items-center justify-between';

/**
 * A CSS class string for a flex container with content justified between.
 */
export const cnFlexBetweenY: string = 'flex justify-between';

/**
 * A CSS class string for a flex container with a column layout.
 */
export const cnFlexCol: string = 'flex flex-col';

/**
 * A CSS class string for a flex container with content horizontally centered.
 */
export const cnFlexCenterX: string = 'flex justify-center';

/**
 * A CSS class string for a flex container with items vertically centered.
 */
export const cnFlexCenterY: string = 'flex items-center';

/**
 * A CSS class string for a flex container with items both vertically
 * and horizontally centered.
 */
export const cnFlexFullCenter = 'flex items-center justify-center';
